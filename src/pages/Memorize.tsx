import { Layout } from "./Layout";

// eslint-disable-next-line @typescript-eslint/no-unused-vars
// @ts-expect-error error
const song = `このおもいを けしてしまうには
まだ
`;

export default function Memorize() {
  return <Layout>Memorize page</Layout>;
}
